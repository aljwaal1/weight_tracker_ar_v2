name: weight_tracker_ar
description: Arabic weight tracker Android APK direct
publish_to: 'none'
version: 5.0.0+5

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true
