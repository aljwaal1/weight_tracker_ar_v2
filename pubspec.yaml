name: weight_tracker_ar
description: measurement tracker Android APK direct
publish_to: 'none'
version: 4.0.0+4

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true
